package org.example.sql;

// Interfejs Persistable definiuje dla obiektów które mogą być zapisywane w bazie danych
public interface Persistable {
    // Metoda getId() zwraca identyfikator obiektu
    Integer getId();
}
